<%= form_for(@api_shortner, url: {controller: "api_shortners", action: "create"}, remote: true) do |f| %>
  <% if @api_shortner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_shortner.errors.count, "error") %> prohibited this link from being saved:</h2>
      <ul>
      <% @api_shortner.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="input-group">
    <%= f.text_field :input_url, :class => "form-control", :placeholder => "Shorten links" %>
    <span class="input-group-btn">
          <%= f.submit 'Shorten Me', :class => "btn btn-primary"%>
      </span
  </div>
<% end %>
